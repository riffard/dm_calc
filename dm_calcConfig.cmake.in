
# - Config file for the dm_calc package
# It defines the following variables

 
# Compute paths
get_filename_component(dm_calc_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(dm_calc_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
 
# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT dm_calc_BINARY_DIR)
  include("${dm_calc_CMAKE_DIR}/dm_calcTargets.cmake")
endif()
 
# These are IMPORTED targets created by dm_calcTargets.cmake
set(dm_calc_LIBRARIES dm_calc)
